import Link from "next/link";
import { ArrowRight, Heart } from "lucide-react";
import { getSiteContent } from "@/lib/content-server";

export default async function DonasiPage() { const c = await getSiteContent(); return <main><section className="page-intro"><div className="container"><span className="section-kicker light">Dukungan</span><h1>{c.donation.title}</h1><p>{c.donation.description}</p></div></section><section className="section"><div className="container donation-card"><div className="donation-icon"><Heart fill="currentColor"/></div><div><span className="section-kicker">Sedekah jariyah</span><h2>Setiap dukungan berarti</h2><p>Partisipasi masyarakat menjadi energi bagi keberlanjutan pendidikan dan pembinaan santri Al Huda. Informasi rekening atau bentuk dukungan dapat diperoleh langsung dari pengelola yayasan.</p><div className="button-row">{c.donation.url ? <a className="button button-primary" href={c.donation.url} target="_blank" rel="noreferrer">Salurkan dukungan <ArrowRight size={17}/></a> : <Link className="button button-primary" href="/kontak">Hubungi pengelola <ArrowRight size={17}/></Link>}<Link className="button button-outline" href="/profil">Tentang yayasan</Link></div></div></div></section></main> }
