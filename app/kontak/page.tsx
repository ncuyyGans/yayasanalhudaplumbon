import Link from "next/link";
import { ArrowUpRight, Instagram, Mail, MapPin, MessageCircle, Phone } from "lucide-react";
import { getSiteContent } from "@/lib/content-server";

export default async function KontakPage() { const c = await getSiteContent(); return <main><section className="page-intro"><div className="container"><span className="section-kicker light">Kontak dan lokasi</span><h1>Mari terhubung dengan Al Huda</h1><p>Silakan hubungi pengelola untuk informasi pendidikan, kerja sama, pendaftaran, dan dukungan program.</p></div></section><section className="section"><div className="container contact-grid"><div><span className="section-kicker">Hubungi kami</span><h2>Kami siap mendengar</h2><div className="contact-list"><ContactItem icon={<MapPin/>} label="Alamat" value={c.contact.address}/><ContactItem icon={<Phone/>} label="Telepon" value={c.contact.phone}/>{c.contact.email && <ContactItem icon={<Mail/>} label="Email" value={c.contact.email}/>}</div><div className="button-row"><a className="button button-primary" href={c.contact.whatsappUrl} target="_blank" rel="noreferrer"><MessageCircle size={17}/>Chat WhatsApp</a><a className="button button-outline" href={c.social.instagram} target="_blank" rel="noreferrer"><Instagram size={17}/>Instagram</a></div></div><div className="map-card"><div className="map-placeholder"><div className="map-pin"><MapPin size={23}/></div><span>Pamijahan<br/><b>Plumbon, Cirebon</b></span></div><a className="map-link" href={c.contact.mapUrl} target="_blank" rel="noreferrer">Buka di Google Maps <ArrowUpRight size={16}/></a></div></div></section><section className="section section-soft"><div className="container contact-bottom"><div><span className="section-kicker">Pendaftaran</span><h2>Informasi PPDB</h2><p>Untuk PPDB, gunakan formulir atau kontak resmi yang ditentukan oleh pengelola.</p></div>{c.ppdbUrl ? <a className="button button-primary" href={c.ppdbUrl} target="_blank" rel="noreferrer">Buka formulir PPDB <ArrowUpRight size={16}/></a> : <Link className="button button-primary" href="/kontak">Tanyakan ke pengelola <ArrowUpRight size={16}/></Link>}</div></section></main> }
function ContactItem({icon,label,value}:{icon:React.ReactNode;label:string;value:string}) { return <div className="contact-item"><div className="contact-icon">{icon}</div><div><span>{label}</span><strong>{value}</strong></div></div> }
