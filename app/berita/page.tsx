import { ArrowRight } from "lucide-react";
import { getSiteContent } from "@/lib/content-server";

export default async function BeritaPage() { const c = await getSiteContent(); return <main><section className="page-intro"><div className="container"><span className="section-kicker light">Berita dan galeri</span><h1>Kabar terbaru dari Al Huda</h1><p>Informasi, kegiatan, dan cerita dari lingkungan Yayasan Pondok Pesantren Al Huda.</p></div></section><section className="section"><div className="container"><div className="news-grid news-grid-page">{c.news.map((item) => <article className="news-card" key={item.title}><div className="news-image"><span>{item.category}</span><div className="news-pattern"/></div><div className="news-body"><span className="news-date">{item.date}</span><h3>{item.title}</h3><p>{item.excerpt}</p><a className="text-link" href={c.social.instagram} target="_blank" rel="noreferrer">Ikuti kabarnya <ArrowRight size={15}/></a></div></article>)}</div></div></section></main> }
