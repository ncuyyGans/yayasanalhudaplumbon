import { ArrowRight, Images } from "lucide-react";
import { getSiteContent } from "@/lib/content-server";

export default async function BeritaPage() {
  const c = await getSiteContent();
  return <main><section className="page-intro"><div className="container"><span className="section-kicker light">Berita dan galeri</span><h1>Kabar terbaru dari Al Huda</h1><p>Informasi, kegiatan, dan cerita dari lingkungan Yayasan Pondok Pesantren Al Huda.</p></div></section><section className="section"><div className="container"><div className="news-grid news-grid-page">{c.news.map((item, i) => <article className="news-card" key={`${item.title}-${i}`}><div className={item.imageUrl ? "news-image has-photo" : "news-image"} style={item.imageUrl ? { backgroundImage: `url(${item.imageUrl})` } : undefined}><span>{item.category}</span>{!item.imageUrl && <div className="news-pattern"/>}</div><div className="news-body"><span className="news-date">{item.date}</span><h3>{item.title}</h3><p>{item.excerpt}</p><a className="text-link" href={c.social.instagram} target="_blank" rel="noreferrer">Ikuti kabarnya <ArrowRight size={15}/></a></div></article>)}</div></div></section>{c.gallery.length > 0 && <section className="section section-soft"><div className="container"><div className="section-heading"><div><span className="section-kicker">Galeri kegiatan</span><h2>Momen dari lingkungan Al Huda</h2></div><Images className="gallery-heading-icon"/></div><div className="public-gallery">{c.gallery.filter((item) => item.imageUrl).map((item) => <figure key={item.id}><img src={item.imageUrl} alt={item.title}/><figcaption><strong>{item.title}</strong>{item.caption && <span>{item.caption}</span>}</figcaption></figure>)}</div></div></section>}</main>;
}
